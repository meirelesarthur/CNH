
  # SIGABEM SISTEMA

  This is a code bundle for SIGABEM SISTEMA. The original project is available at https://www.figma.com/design/I56qHoIfVILmTls9uXdGNS/SIGABEM-SISTEMA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  